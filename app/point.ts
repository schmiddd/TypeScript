export class Point {

    /**
     * This constructor shows the shortened versions to define properties.
     * Similar to C# notation. Adding the visibility modifier will generate get and set for the property.
     * 
     * @param X - longitude in degree
     * @param Y - latitude in degree
     */
    constructor(public X: number, public Y: number) {
    }
}